<div class="container mx-auto px-4 py-8">
  <div class="mb-6 flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
    <div>
      <h1 class="text-2xl font-bold text-foreground">Edit career profile</h1>
      <p class="text-sm text-muted-foreground">Keep the application resources you reuse in one place.</p>
    </div>
    <%= link_to "Back to profile", career_profile_path, class: "inline-flex items-center justify-center rounded-md border border-input px-4 py-2" %>
  </div>

  <%= render "form" %>
</div>
