<% if link? %>
  <%= link_to(@href, **tag_options) do %>
    <%= content.present? ? content : @label %>
  <% end %>
<% else %>
  <%= button_tag type: @type, **tag_options do %>
    <%= content.present? ? content : @label %>
  <% end %>
<% end %>
